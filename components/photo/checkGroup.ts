const checkGroup = (existPosting, groupId) => {
  //   !!existPosting &&
  //     existPosting?.constructor === Object &&
  //     Object.keys(existPosting).length !== 0 &&
  //   const posting = existPosting.map((posting) => {
  //     if (posting[0].category == groupId) return posting;
  //   });

  console.log(existPosting.map((posting) => console.log(posting)));
};

export default checkGroup;
